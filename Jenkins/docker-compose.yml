version: '3.8'
services:
  jenkins:
    container_name: jenkins
    build:
      context: ./
      dockerfile: jenkins.dockerfile

    ports:
      - '8080:8080'
      - '50000:50000'

    networks:
      - jenkins

    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
    
    command: sh -c "chown root:docker /var/run/docker.sock"